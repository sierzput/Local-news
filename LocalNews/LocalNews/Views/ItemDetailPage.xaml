<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LocalNews.Views.ItemDetailPage"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:viewModels="clr-namespace:LocalNews.ViewModels;assembly=LocalNews"
             d:DataContext="{d:DesignInstance viewModels:ItemsViewModel}"
             Title="{Binding Title}">
	<StackLayout Spacing="20" Padding="15" d:DataContext="{d:DesignInstance viewModels:ItemDetailViewModel}">
		<Label Text="{Binding Item.Title}" FontSize="Medium"/>
        <Label Text="{Binding Item.Summary}" FontSize="Small"/>
    </StackLayout>
</ContentPage>